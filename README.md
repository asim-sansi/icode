# icode 
icode is a project idea aims to assist front-end web developers with the basic HTML code automation, it exctracts the web components using image processing technuiqes and then a classification clssifies the extracted components as corresponding HTML elements, we then create a merkel tree and gereate HTML script.
